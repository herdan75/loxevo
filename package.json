{
  "name": "loxevo",
  "version": "0.1.0",
  "description": "Eigene LoxBerry-Zentrale für Alexa, Echo-TTS und Loxone.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/command-utils.js && node --check src/loxone.js && node --check src/tts.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
